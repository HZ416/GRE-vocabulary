export function PlaceholderPage({ title }: { title: string }) {
  return (
    <section>
      <header className="page-header">
        <h1>{title}</h1>
        <p>This section is ready for the next implementation phase.</p>
      </header>
    </section>
  )
}


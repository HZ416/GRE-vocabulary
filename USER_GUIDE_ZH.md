# GRE Vocabulary 完整中文使用教程

本教程适用于 GRE Vocabulary 0.1.x 桌面版，面向日常使用者，同时在最后提供从源码运行和自行打包的方法。

## 目录

1. [先了解这个程序](#1-先了解这个程序)
2. [下载适合自己的安装包](#2-下载适合自己的安装包)
3. [在 macOS 上安装](#3-在-macos-上安装)
4. [在 Windows 上安装](#4-在-windows-上安装)
5. [首次启动与中文界面设置](#5-首次启动与中文界面设置)
6. [导入已经准备好的单词表](#6-导入已经准备好的单词表)
7. [自己制作 CSV 词库](#7-自己制作-csv-词库)
8. [每天怎样学习](#8-每天怎样学习)
9. [词库、搜索和单词详情](#9-词库搜索和单词详情)
10. [收藏与难词](#10-收藏与难词)
11. [统计页面](#11-统计页面)
12. [设置页面](#12-设置页面)
13. [数据保存在哪里](#13-数据保存在哪里)
14. [导出、备份和恢复](#14-导出备份和恢复)
15. [换电脑时迁移数据](#15-换电脑时迁移数据)
16. [常见问题](#16-常见问题)
17. [从源码运行](#17-从源码运行)
18. [自己构建安装包](#18-自己构建安装包)
19. [当前版本的限制](#19-当前版本的限制)

## 1. 先了解这个程序

GRE Vocabulary 是一个本地优先的 GRE 单词学习程序，主要功能包括：

- 从 CSV 文件导入自己的词库；
- 浏览并搜索英文单词、英文释义和中文释义；
- 使用 FSRS 间隔重复算法安排新词和复习；
- 用“重来、困难、良好、简单”记录掌握程度；
- 标记收藏词和难词；
- 为每个单词保存个人笔记；
- 查看学习数量、正确率和连续学习天数；
- 导出词库与进度，并备份或恢复完整数据库；
- 在中文和英文界面之间切换。

需要先明确四点：

1. 程序不会自动附带商业 GRE 词库，首次使用需要导入 CSV。
2. 单词、笔记和学习记录默认只保存在当前电脑，不会上传云端。
3. 当前没有账号、云同步和手机同步功能。
4. 当前 macOS 和 Windows 安装包均未进行付费的正式代码签名，仅适合自己使用或在可信范围内分发。

## 2. 下载适合自己的安装包

项目地址：<https://github.com/HZ416/GRE-vocabulary>

打开项目的 [Desktop build 工作流](https://github.com/HZ416/GRE-vocabulary/actions/workflows/desktop-build.yml)，选择最近一次带绿色对勾的运行记录，在页面底部的 **Artifacts** 区域下载对应压缩包。

| 你的电脑 | 应下载的构建产物 | 安装文件 |
| --- | --- | --- |
| Apple 芯片 Mac（M1、M2、M3、M4 等） | `gre-vocabulary-macOS-Apple-Silicon` | `.dmg` |
| Intel 芯片 Mac | `gre-vocabulary-macOS-Intel` | `.dmg` |
| 64 位 Windows | `gre-vocabulary-Windows-x64` | `.exe` 或 `.msi` |

GitHub 下载的 Artifact 本身通常是 `.zip`，需要先解压，再打开里面的安装文件。下载 Artifact 可能需要登录 GitHub。

### 如何判断 Mac 芯片类型

点击屏幕左上角苹果菜单，选择“关于本机”：

- 显示“芯片 Apple M…”时，选择 Apple Silicon；
- 显示“处理器 Intel…”时，选择 Intel。

也可以在终端运行：

```sh
uname -m
```

- `arm64` 表示 Apple Silicon；
- `x86_64` 表示 Intel。

## 3. 在 macOS 上安装

1. 双击下载并解压后的 `.dmg` 文件。
2. 将 **GRE Vocabulary** 拖到“应用程序”文件夹。
3. 在“应用程序”中找到 GRE Vocabulary。
4. 第一次启动时，建议按住 Control 点击应用图标，选择“打开”，再在确认窗口中点击“打开”。

由于安装包尚未使用 Apple Developer ID 正式签名和公证，macOS 可能提示无法验证开发者。如果右键“打开”仍被阻止：

1. 打开“系统设置”；
2. 进入“隐私与安全性”；
3. 找到刚刚被阻止的 GRE Vocabulary；
4. 确认文件确实来自本项目后，选择“仍要打开”。

不同 macOS 版本的按钮文字可能略有不同。只应对自己构建或从本项目可信运行记录下载的文件执行这一步。

## 4. 在 Windows 上安装

Windows 构建产物通常同时包含两种安装程序：

- `.exe`：NSIS 安装程序，通常更适合普通用户；
- `.msi`：Windows Installer 安装包，也可以使用。

安装方法：

1. 解压 GitHub Artifact；
2. 双击 `.exe` 或 `.msi`；
3. 按安装向导完成安装；
4. 从开始菜单启动 GRE Vocabulary。

因为安装包没有 Windows 代码签名，Microsoft Defender SmartScreen 可能显示保护提示。确认文件来自自己的 GitHub 仓库和成功的构建任务后，可选择“更多信息”，再选择“仍要运行”。

## 5. 首次启动与中文界面设置

程序第一次启动时默认显示英文界面。

### 切换成中文

1. 点击左侧导航栏的 **Settings**；
2. 找到 **Interface language**；
3. 选择“中文”；
4. 点击页面下方的“保存偏好”。

语言选择保存在本地数据库中，下次启动时仍会保持中文。

### 推荐的首次设置

在“设置”页面中可以先使用以下配置：

- 每日新词数：`20`；
- 每日最大复习数：`200`；
- 英文释义：开启；
- 中文释义：开启；
- IPA 音标：开启；
- 例句：开启。

每日新词可设置为 `0–200`，每日最大复习数可设置为 `1–1000`。英文释义和中文释义至少要保留一项开启。

### 检查数据库

在“设置”页面底部找到“数据库健康”，点击“运行健康检查”。出现“健康 · SQLite …”说明本地数据库连接正常。

## 6. 导入已经准备好的单词表

如果你使用本项目准备的个人单词索引，优先选择：

```text
单词索引-含释义.csv
```

这个文件包含单词、词性、英文释义、可匹配到的中文翻译、音标、例句和来源信息。简版的 `单词索引-可导入.csv` 只有单词和来源，一般不必再使用。

导入步骤：

1. 打开左侧“词库”；
2. 点击右上方“导入 CSV”；
3. 选择 `单词索引-含释义.csv`；
4. 等待导入完成；
5. 页面会显示新增、更新和合并的重复行数量；
6. 返回“概览”，确认“总计”和词库进度已经出现。

当前准备好的文件包含 6,862 个单词。由于中文字段来自开放英中词典的自动匹配，并不是人工审校的 GRE 专用释义，所以部分词只有英文释义，少量中文翻译也可能偏窄。学习时可在单词详情的“个人笔记”中写下自己确认过的常考义。

### 已经导入过简版 CSV 怎么办

可以继续导入含释义版本。程序会按单词合并数据：

- 已有学习进度、收藏、难词和笔记不会丢失；
- 已有来源会与新来源合并；
- 原来为空的释义、词性、音标和例句会被补充；
- 已经存在的非空释义不会被新导入内容覆盖。

因此，重新导入适合用于补齐空字段，但不能用于替换一个已经存在的错误释义。需要整体回退时应使用导入前的完整数据库备份。

## 7. 自己制作 CSV 词库

### 最简单的方法

在空词库状态下打开“词库”，点击“保存 CSV 模板”。程序会生成包含全部支持字段的 UTF-8 CSV 模板。

CSV 最少需要两个字段：

```csv
lemma,source_name
equivocal,personal_list
laconic,personal_list
```

推荐使用完整格式：

```csv
lemma,part_of_speech,definition_en,definition_zh,ipa,example_sentence,source_name,source_group,source_rank,is_high_priority
equivocal,adjective,open to several meanings,模棱两可的,/ɪˈkwɪvəkəl/,The statement was deliberately equivocal.,personal_list,my_gre_words,1,1
```

### 字段说明

| 字段 | 是否必需 | 说明 |
| --- | --- | --- |
| `lemma` | 必需 | 单词或词条，例如 `equivocal` |
| `source_name` | 必需 | 来源名称，例如 `personal_list` |
| `part_of_speech` | 可选 | 词性，例如 `adjective`、`noun` |
| `definition_en` | 可选 | 英文释义 |
| `definition_zh` | 可选 | 中文释义或翻译 |
| `ipa` | 可选 | IPA 音标 |
| `example_sentence` | 可选 | 例句 |
| `source_group` | 可选 | 来源分组 |
| `source_rank` | 可选 | 非负整数形式的来源排名 |
| `is_high_priority` | 可选 | `1`、`true` 或 `yes` 表示高优先级 |

### CSV 制作注意事项

- 第一行必须是字段名；
- 每行放一个词条；
- 文件应保存为 UTF-8，带 UTF-8 BOM 也可以；
- 字段名必须使用英文并保持上表拼写；
- 包含逗号、双引号或换行的字段必须遵守标准 CSV 引号规则；
- 不要从商业词典批量复制没有再分发许可的释义和例句；
- 同一文件中的重复单词会自动合并，并在导入结果中显示重复数量。

程序会把单词统一为小写、统一弯引号和普通单引号，并压缩多余空格。来源名称会统一为小写下划线形式。

## 8. 每天怎样学习

### 8.1 查看今日任务

打开“概览”可以看到：

- 新词：今天允许加入学习的未见单词；
- 到期：今天已经到复习时间的单词；
- 逾期：以前到期但还没完成的单词；
- 总计：今天队列中的新词、到期词和逾期词总数。

点击“开始复习”进入学习页面。系统先安排已到期的复习词，再加入当天的新词。新词优先考虑来源优先级，然后按字母排序。

### 8.2 查看答案

卡片正面显示单词，并根据设置决定是否显示音标。先在脑中回忆，再执行以下任一操作：

- 点击“显示答案”；
- 按键盘空格键。

显示答案后，可以看到已启用的英文释义、中文释义和例句。

### 8.3 选择评分

| 评分 | 快捷键 | 建议使用场景 |
| --- | --- | --- |
| 重来 | `1` | 没想起来，或者答案基本错误 |
| 困难 | `2` | 想起来了，但非常吃力或不确定 |
| 良好 | `3` | 正常回忆出来，是最常用的评分 |
| 简单 | `4` | 很快、很确定地想起来 |

只有显示答案后评分按钮和 `1–4` 快捷键才会生效。不要为了减少复习量而频繁选择“简单”；按真实回忆情况评分，FSRS 才能安排合理的下次复习时间。

### 8.4 收藏和难词快捷键

复习卡片中：

- 按 `F` 切换收藏；
- 按 `D` 切换难词；
- 也可以点击卡片顶部对应按钮。

### 8.5 “已掌握”是什么意思

程序使用 FSRS 保存每个词的稳定度和下次到期时间。当前版本在一个单词至少复习 4 次，并且稳定度达到约 30 天时，将它显示为“已掌握”。已掌握不等于永远不再出现；到达新的复习日期后，它仍可能进入队列。

## 9. 词库、搜索和单词详情

### 浏览和搜索

打开“词库”可以查看单词、释义、级别、状态和优先级。

搜索框支持搜索：

- 英文单词；
- 英文释义；
- 中文释义。

输入内容后点击“搜索”或按回车。当前页面一次最多显示 500 个匹配结果，建议输入更具体的关键词缩小范围。

### 单词详情

点击单词进入详情页，可以查看：

- 音标和词性；
- 英文与中文释义；
- 当前学习状态、优先级和复习次数；
- 例句；
- 数据来源；
- 个人笔记。

### 使用个人笔记

个人笔记适合记录：

- 自己确认过的 GRE 常考义；
- 词根词缀；
- 助记方法；
- 易混词区别；
- 自己做错过的题目线索。

输入后点击“保存笔记”。笔记只保存在本机，并包含在完整数据库备份和词库导出中。

当前版本不能直接在界面中编辑导入的释义，也不能从界面删除单词。

## 10. 收藏与难词

左侧导航栏提供两个独立列表：

- “收藏”：保存想快速找到的单词；
- “难词”：集中查看需要重点学习的单词。

同一个词可以同时属于收藏和难词。在对应列表中点击“移除”只会取消该标记，不会删除单词，也不会清除学习进度。

## 11. 统计页面

“统计”页面显示：

- 已学习单词数；
- 已复习单词数；
- 已掌握单词数；
- 今日复习次数；
- 今日正确率；
- 连续学习天数；
- 全部复习次数；
- 总正确率。

评分“困难、良好、简单”计入正确，“重来”计入错误。连续学习按本机日期计算；如果今天还没有复习，会从昨天开始检查连续天数。

## 12. 设置页面

“设置”包含三部分。

### 学习偏好

- 界面语言：中文或 English；
- 每日新词数：控制每天最多加入多少未见词；
- 每日最大复习数：控制到期复习词的队列上限；
- 答案内容：控制英文释义、中文释义、音标和例句的显示。

调整后必须点击“保存偏好”。这些设置保存在本地数据库，也包含在进度 JSON 和完整数据库备份中。

### 导出与备份

可以导出词库 CSV、词库 JSON、进度 JSON、完整数据库，也可以恢复完整数据库备份。详细区别见下一节。

### 数据库健康

健康检查用于确认应用能够连接本地 SQLite 数据库。它不是完整备份的替代品；重要学习数据仍应定期备份。

## 13. 数据保存在哪里

程序的主要数据保存在 SQLite 文件：

```text
gre-vocabulary.db
```

默认位置：

### macOS

```text
~/Library/Application Support/com.hz416.gre-vocabulary/gre-vocabulary.db
```

在 Finder 中可以选择“前往 → 前往文件夹”，输入：

```text
~/Library/Application Support/com.hz416.gre-vocabulary/
```

### Windows

```text
%APPDATA%\com.hz416.gre-vocabulary\gre-vocabulary.db
```

可以在文件资源管理器地址栏粘贴：

```text
%APPDATA%\com.hz416.gre-vocabulary
```

数据库包含词库、来源、个人笔记、收藏、难词、设置、FSRS 状态和完整复习历史。

不要在程序运行时手动复制、替换或编辑活动数据库。请优先使用程序“设置 → 导出与备份”中的功能。

## 14. 导出、备份和恢复

### 四种导出有什么区别

| 功能 | 默认文件名 | 包含内容 | 适合用途 |
| --- | --- | --- | --- |
| 词库 CSV | `gre-vocabulary.csv` | 单词、释义、笔记、来源等 | 用 Excel 查看、编辑或重新导入 |
| 词库 JSON | `gre-vocabulary.json` | 结构化的完整词库和来源 | 程序处理、长期归档 |
| 进度 JSON | `gre-progress.json` | 设置、单词状态、FSRS 状态、复习日志 | 分析学习进度 |
| 完整数据库备份 | `gre-vocabulary-backup.db` | 当前应用的全部本地数据 | 灾难恢复和换电脑迁移 |

词库 CSV/JSON 和进度 JSON 是便携数据导出，但当前界面只支持导入词库 CSV。要在另一台电脑上完整恢复所有内容，应使用 `.db` 完整数据库备份。

### 创建完整备份

1. 打开“设置”；
2. 找到“导出与备份”；
3. 点击“完整数据库备份”；
4. 选择保存位置；
5. 将生成的 `.db` 文件复制到安全位置。

建议至少在以下时间备份：

- 第一次成功导入大词库后；
- 大批量重新导入之前；
- 每周或每完成一阶段学习后；
- 更新应用版本之前；
- 更换电脑之前。

### 恢复完整备份

恢复会替换当前全部词库、笔记、设置和学习进度，请先备份当前数据。

1. 打开“设置”；
2. 点击“恢复数据库备份”；
3. 选择 `.db`、`.sqlite` 或 `.sqlite3` 文件；
4. 阅读警告并确认；
5. 等待验证和恢复完成；
6. 程序会自动刷新。

恢复前，程序会检查 SQLite 完整性、数据库结构、版本、必需字段、设置和外键关系。替换当前数据库前，还会自动创建：

```text
gre-vocabulary-before-restore.db
```

如果替换后的最终验证失败，程序会尝试自动恢复原数据库。由更新版本程序创建的较新数据库，可能无法恢复到旧版本程序中。

## 15. 换电脑时迁移数据

推荐流程：

1. 在旧电脑中创建“完整数据库备份”；
2. 把 `.db` 备份复制到移动硬盘、局域网或自己的安全云盘；
3. 在新电脑安装并至少启动一次 GRE Vocabulary；
4. 在新电脑打开“设置 → 恢复数据库备份”；
5. 选择从旧电脑复制过来的 `.db` 文件；
6. 恢复后检查总词数、收藏、难词、个人笔记和统计数据；
7. 再创建一次新电脑上的完整备份。

这种方式可以在 macOS 与 Windows 之间迁移，因为备份使用标准 SQLite 格式，但两台电脑最好运行相同或更新的应用版本。

## 16. 常见问题

### 16.1 为什么打开后没有单词

程序不内置商业词库。请打开“词库”，导入 UTF-8 CSV。个人准备好的版本可选择 `单词索引-含释义.csv`。

### 16.2 导入时提示缺少 `lemma` 或 `source_name`

CSV 第一行缺少必需字段，或者字段名拼写不正确。至少应为：

```csv
lemma,source_name
```

### 16.3 提示某一行 CSV 格式错误

常见原因包括：

- 释义中有逗号但没有使用 CSV 引号；
- 单元格中包含未正确转义的双引号；
- 文件不是标准 CSV；
- 从表格软件导出时选择了错误的分隔符或编码。

最稳妥的方法是先由程序保存模板，再用 Excel、Numbers 或 LibreOffice 编辑，并导出为 UTF-8 CSV。

### 16.4 为什么重新导入后释义没有变化

为了保护已经使用的数据，重新导入只会补充原本为空的字段，不会覆盖已有非空释义。若需要整体撤销一次错误导入，应恢复导入前的完整数据库备份。

### 16.5 为什么部分单词没有中文释义

个人索引的开放词典中文匹配覆盖率低于英文释义，未可靠匹配的字段会留空。可以继续依赖英文释义学习，并把自己确认的中文常考义写入个人笔记。

### 16.6 为什么今天没有学习卡片

可能原因：

- 今日到期和逾期词已经完成；
- “每日新词数”被设置为 `0`；
- 词库尚未导入；
- 今日队列已达到设置上限。

先检查“概览”的新词、到期、逾期和总计，再检查设置中的每日上限。

### 16.7 为什么统计正确率和我的感觉不同

当前规则把“重来”记为错误，把“困难、良好、简单”记为正确。统计是按每次评分记录计算，不是按独立单词计算。

### 16.8 数据库健康检查失败怎么办

1. 关闭并重新打开程序；
2. 确认应用配置目录有写入权限；
3. 不要让同步软件或其他程序同时锁定数据库；
4. 如果仍失败，保留现有应用数据目录，不要直接删除；
5. 尝试恢复最近一次完整数据库备份。

### 16.9 怎样完全重新开始

当前没有“清空全部数据”按钮。若确实需要重新开始：

1. 先创建完整数据库备份；
2. 完全退出程序；
3. 将整个 `com.hz416.gre-vocabulary` 应用配置文件夹移动到一个安全备份位置，不要直接删除；
4. 重新启动程序，让它创建新数据库；
5. 确认新数据库正常后，再决定是否保留旧文件夹。

这个操作会让当前词库和进度暂时从程序中消失，务必先备份。

### 16.10 macOS 或 Windows 阻止程序运行

这是因为当前安装包没有正式代码签名。请只使用自己构建或来自可信 GitHub Actions 成功任务的文件，然后按本教程第 3、4 节的方法确认打开。

## 17. 从源码运行

### 环境要求

- Node.js 24 LTS；
- npm；
- Rust stable 工具链；
- Tauri 所需的系统构建依赖。

克隆并进入项目：

```sh
git clone https://github.com/HZ416/GRE-vocabulary.git
cd GRE-vocabulary
```

安装前端依赖：

```sh
npm install
```

启动桌面开发版：

```sh
npm run tauri dev
```

第一次构建会下载并编译 Rust 依赖，所需时间通常明显长于之后的启动。

### 运行检查

```sh
npm test
npm run test:performance
npx playwright install chromium
npm run test:e2e
npm run typecheck
npm run lint
npm run build
cargo test --manifest-path src-tauri/Cargo.toml
```

只想日常使用时不需要运行这些开发命令，直接安装桌面构建产物即可。

## 18. 自己构建安装包

安装好 Node.js、Rust 和 Tauri 系统依赖后，在项目根目录运行：

```sh
npm install
npm run tauri build
```

常见输出位置：

### macOS

```text
src-tauri/target/release/bundle/dmg/
```

指定 Apple Silicon 或 Intel 目标时，文件会位于相应目标目录，例如：

```text
src-tauri/target/aarch64-apple-darwin/release/bundle/dmg/
src-tauri/target/x86_64-apple-darwin/release/bundle/dmg/
```

### Windows

```text
src-tauri/target/release/bundle/nsis/
src-tauri/target/release/bundle/msi/
```

项目的 GitHub Actions 会在每次推送到 `main` 后运行测试，并分别构建：

- macOS Apple Silicon；
- macOS Intel；
- Windows x64。

这些自动构建目前仍是未正式签名的个人使用版本。若要公开分发，需要另外配置 Apple Developer ID 签名与公证，以及 Windows 代码签名证书。

## 19. 当前版本的限制

- 没有内置经过完整授权和人工审校的 800–1,200 词 GRE 核心词库；
- 个人 6,862 词索引是扩展词表，不等同于按考试频率筛选的核心词表；
- 开放词典的中文覆盖和 GRE 常考义匹配仍需人工校正；
- 没有云同步、账号系统和移动端；
- 没有单词发音音频或文字转语音；
- 没有在界面内编辑释义、删除单词或批量管理词条的功能；
- 没有句子填空、同义句选择等 GRE 题型训练；
- 没有正式签名的 macOS 和 Windows 安装包。

对于个人使用，推荐先导入含释义词表，把每日新词设置为 `10–20`，按真实回忆情况完成每日复习，并每周保存一次完整数据库备份。
